// Placeholder para rota de login
// Em produção, isso seria implementado com Replit Auth
export default function handler(req, res) {
  res.status(200).json({ message: 'Login endpoint - implementar Replit Auth' });
}